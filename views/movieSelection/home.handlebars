<h1>{{title.title}}</h1>
<img src={{img}} alt={{title}}>
<h3>{{message}}</h3>
<button class={{enter_button}}>Enter decision room!</button>
<form class="{{pick}}" action="/movieSelection/list" method="GET">
   <input type="submit" value="Pick Flicks">
</form>
<!-- Where should this lead? -->
<form class="{{exit}}" action="/" method="GET">
   <input type="submit" value="Exit">
</form>